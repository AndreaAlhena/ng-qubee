name: CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --browsers=ChromeHeadless --watch=false --code-coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        if: success()
        with:
          files: ./coverage/ng-qubee/lcov.info
          flags: unittests
          name: ng-qubee-coverage
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build library
        run: npm run build

      - name: Check build artifacts
        run: |
          if [ ! -d "dist/ng-qubee" ]; then
            echo "Build failed: dist/ng-qubee directory not found"
            exit 1
          fi
          echo "Build successful: dist/ng-qubee directory exists"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: dist/ng-qubee
          retention-days: 7

  compat:
    name: Angular ${{ matrix.angular }} compat
    runs-on: ubuntu-latest
    needs: build

    strategy:
      fail-fast: false
      matrix:
        include:
          # Floor of the declared peer range
          - angular: '17'
            node: '20'
          - angular: '20'
            node: '22'
          # Ceiling of the declared peer range
          - angular: '22'
            node: '24'

    env:
      NG_CLI_ANALYTICS: 'false'

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: ng-qubee-dist

      - name: Pack the built library
        run: npm pack --pack-destination "$GITHUB_WORKSPACE"
        working-directory: ng-qubee-dist

      - name: Scaffold an Angular ${{ matrix.angular }} application
        run: |
          npx --yes @angular/cli@${{ matrix.angular }} new consumer \
            --routing=false --style=css --ssr=false --skip-git --defaults \
            --package-manager=npm

      - name: Install the packed library
        run: npm install "$GITHUB_WORKSPACE"/ng-qubee-*.tgz
        working-directory: consumer

      - name: Consume the library for real
        run: |
          cat > src/main.ts <<'TS'
          import { Component, inject } from '@angular/core';
          import { bootstrapApplication } from '@angular/platform-browser';
          import { DriverEnum, NgQubeeService, provideNgQubee } from 'ng-qubee';

          @Component({
            selector: 'app-root',
            standalone: true,
            template: '<pre>{{ uri }}</pre>'
          })
          export class AppRoot {
            private readonly _queryBuilder = inject(NgQubeeService);

            public uri = '';

            constructor() {
              this._queryBuilder
                .setResource('users')
                .addFilter('status', 'active')
                .setLimit(15)
                .setPage(2);

              this._queryBuilder.generateUri().subscribe(uri => (this.uri = uri));
            }
          }

          bootstrapApplication(AppRoot, {
            providers: [provideNgQubee({ driver: DriverEnum.SPATIE })]
          });
          TS
        working-directory: consumer

      - name: Build the consumer application
        run: npx ng build
        working-directory: consumer

      - name: Verify the library was linked and not tree-shaken
        run: |
          BUNDLE=$(find dist -name '*.js' -print0 | xargs -0 grep -l 'spatie' 2>/dev/null | head -1 || true)
          if [ -z "$BUNDLE" ]; then
            echo "ng-qubee was tree-shaken out — the build proved nothing"
            exit 1
          fi
          echo "Library code present in $BUNDLE"

          if grep -rq 'ngDeclare' dist; then
            echo "Residual partial declarations found — the Angular ${{ matrix.angular }} linker did not process them"
            exit 1
          fi
          echo "All partial declarations linked by Angular ${{ matrix.angular }}"
        working-directory: consumer

  all-checks-passed:
    name: All Checks Passed
    runs-on: ubuntu-latest
    needs: [lint, test, build, compat]
    if: always()

    steps:
      - name: Check all jobs status
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]] || \
             [[ "${{ needs.test.result }}" != "success" ]] || \
             [[ "${{ needs.build.result }}" != "success" ]] || \
             [[ "${{ needs.compat.result }}" != "success" ]]; then
            echo "One or more checks failed"
            exit 1
          fi
          echo "All checks passed successfully!"
